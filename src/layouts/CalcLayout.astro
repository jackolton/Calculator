---
import '../styles/tokens.css';
import { softwareApplicationSchema, faqSchema, breadcrumbSchema } from '../lib/schema';
import type { CalcSpec } from '../lib/types';
import { getAllCalcs } from '../lib/calcs';

interface Props {
  spec: CalcSpec;
}
const { spec } = Astro.props;
const allCalcs = getAllCalcs();

const schemaApp  = softwareApplicationSchema(spec);
const schemaFaq  = faqSchema(spec.faq ?? []);
const schemaBc   = breadcrumbSchema(spec.slug, spec.title);
const canonicalUrl = `https://reicalcs.com/${spec.slug}`;
const ogImage = `/og/${spec.slug}.svg`;
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{spec.metaTitle}</title>
  <meta name="description" content={spec.metaDescription} />
  <link rel="canonical" href={canonicalUrl} />

  <meta property="og:title"        content={spec.metaTitle} />
  <meta property="og:description"  content={spec.metaDescription} />
  <meta property="og:image"        content={`https://reicalcs.com${ogImage}`} />
  <meta property="og:url"          content={canonicalUrl} />
  <meta property="og:type"         content="website" />
  <meta name="twitter:card"        content="summary_large_image" />
  <meta name="twitter:title"       content={spec.metaTitle} />
  <meta name="twitter:description" content={spec.metaDescription} />
  <meta name="twitter:image"       content={`https://reicalcs.com${ogImage}`} />

  <script type="application/ld+json" set:html={JSON.stringify(schemaApp)} />
  <script type="application/ld+json" set:html={JSON.stringify(schemaFaq)} />
  <script type="application/ld+json" set:html={JSON.stringify(schemaBc)} />

  <script defer data-domain="reicalcs.com" src="https://plausible.io/js/script.js"></script>
  <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
</head>
<body style="min-height: 100vh;">

  <!-- App bar -->
  <header style="background: var(--color-surface); border-bottom: 1px solid var(--color-divider); position: sticky; top: 0; z-index: 10;">
    <nav style="max-width: 1024px; margin: 0 auto; padding: 0 20px; height: 64px; display: flex; align-items: center; justify-content: space-between;">

      <!-- Logo mark + wordmark -->
      <a href="/" style="display: flex; align-items: center; gap: 10px; text-decoration: none;">
        <span style="width: 28px; height: 28px; background: var(--color-primary); border-radius: 7px; display: flex; align-items: center; justify-content: center; flex-shrink: 0;" aria-hidden="true">
          <svg width="15" height="15" viewBox="0 0 15 15" fill="none">
            <rect x="2" y="2" width="4.5" height="4.5" rx="1" fill="white" opacity="0.9"/>
            <rect x="8.5" y="2" width="4.5" height="4.5" rx="1" fill="white" opacity="0.7"/>
            <rect x="2" y="8.5" width="4.5" height="4.5" rx="1" fill="white" opacity="0.7"/>
            <rect x="8.5" y="8.5" width="4.5" height="4.5" rx="1" fill="white" opacity="0.9"/>
          </svg>
        </span>
        <span style="font-family: var(--font-display); font-size: 17px; font-weight: 700; color: var(--color-text); letter-spacing: -0.03em;">
          REI Calcs
        </span>
      </a>

      <!-- Nav dropdown -->
      <details class="group" style="position: relative;">
        <summary style="cursor: pointer; list-style: none; display: flex; align-items: center; gap: 5px; font-size: 13px; font-weight: 600; color: var(--color-text-muted); padding: 6px 14px; border-radius: var(--radius-pill); transition: background 150ms, color 150ms;" class="hover:bg-google-hover hover:text-google-text marker:content-none">
          All Calcs
          <svg class="h-3.5 w-3.5 transition-transform group-open:rotate-180" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
            <path fill-rule="evenodd" d="M5.22 8.22a.75.75 0 0 1 1.06 0L10 11.94l3.72-3.72a.75.75 0 1 1 1.06 1.06l-4.25 4.25a.75.75 0 0 1-1.06 0L5.22 9.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" />
          </svg>
        </summary>
        <div style="position: absolute; right: 0; top: calc(100% + 6px); width: 240px; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-md); box-shadow: var(--shadow-lift); padding: 4px 0; z-index: 20; animation: fadeIn 0.15s ease both;">
          {allCalcs.map((c) => (
            <a
              href={`/${c.slug}`}
              style="display: block; padding: 10px 16px; font-size: 13px; font-weight: 500; color: var(--color-text); text-decoration: none; transition: background 100ms;"
              class="hover:bg-google-hover"
            >{c.title}</a>
          ))}
        </div>
      </details>
    </nav>
  </header>

  <main style="max-width: 672px; margin: 0 auto; padding: 36px 20px 80px;">

    <!-- Breadcrumb -->
    <nav aria-label="Breadcrumb" class="animate-hero" style="display: flex; align-items: center; gap: 6px; font-size: 11px; font-weight: 500; color: var(--color-text-muted); margin-bottom: 20px; letter-spacing: 0.01em;">
      <a href="/" style="color: var(--color-primary); text-decoration: none; transition: opacity 150ms;" class="hover:opacity-70">REI Calcs</a>
      <span aria-hidden="true" style="color: var(--color-border);">›</span>
      <span>{spec.title}</span>
    </nav>

    <!-- Hero -->
    <div class="animate-hero" style="margin-bottom: 28px;">
      <h1 style="margin-bottom: 10px;">{spec.h1}</h1>
      <p style="font-size: 14px; color: var(--color-text-muted); line-height: 1.65; margin: 0; max-width: 520px;">{spec.tagline || spec.metaDescription}</p>
    </div>

    <!-- Calculator widget -->
    <div class="animate-calc">
      <slot name="calculator" />
    </div>

    <!-- Affiliate CTA -->
    <div class="animate-below" style="margin-top: 16px;">
      <slot name="affiliate" />
    </div>

    <!-- Embed snippet -->
    <details class="group animate-below" style="margin-top: 20px;">
      <summary style="cursor: pointer; list-style: none; display: inline-flex; align-items: center; gap: 6px; font-size: 12px; font-weight: 600; color: var(--color-text-muted); transition: color 150ms;" class="hover:text-google-text marker:content-none">
        <svg class="h-3.5 w-3.5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" d="M17.25 6.75 22.5 12l-5.25 5.25m-10.5 0L1.5 12l5.25-5.25m7.5-3-4.5 16.5" />
        </svg>
        Embed this calculator
      </summary>
      <div style="margin-top: 12px; background: var(--color-bg); border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: 16px;">
        <pre style="font-family: var(--font-mono); font-size: 11px; color: var(--color-text-muted); overflow-x: auto; white-space: pre-wrap; margin: 0;"><code>{`<iframe src="https://reicalcs.com/embed/${spec.slug}" width="100%" height="420" frameborder="0" title="${spec.title}" loading="lazy"></iframe>`}</code></pre>
        <button
          onclick={`navigator.clipboard.writeText('<iframe src="https://reicalcs.com/embed/${spec.slug}" width="100%" height="420" frameborder="0" title="${spec.title}" loading="lazy"></iframe>').then(()=>{this.textContent='Copied!';setTimeout(()=>{this.textContent='Copy embed code'},2000)})`}
          style="margin-top: 10px; font-size: 12px; font-weight: 600; color: var(--color-primary); background: none; border: none; cursor: pointer; padding: 0; font-family: var(--font-body);"
        >
          Copy embed code
        </button>
      </div>
    </details>

    <!-- Explainer content -->
    <article class="prose-drive" style="margin-top: 56px;">
      <slot name="content" />
    </article>

    <!-- FAQ -->
    <slot name="faq" />

    <!-- Related -->
    <slot name="related" />
  </main>

  <footer style="border-top: 1px solid var(--color-divider); padding: 28px 20px; text-align: center;">
    <div style="max-width: 1024px; margin: 0 auto; display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 20px; font-size: 12px; color: var(--color-text-muted); font-family: var(--font-body);">
      <span style="font-weight: 600; font-family: var(--font-display); letter-spacing: -0.02em; color: var(--color-text);">REI Calcs</span>
      <a href="/how-we-make-money" style="color: var(--color-text-muted); text-decoration: none; transition: color 150ms;" class="hover:text-google-text">How we make money</a>
      <a href="/privacy" style="color: var(--color-text-muted); text-decoration: none; transition: color 150ms;" class="hover:text-google-text">Privacy</a>
    </div>
  </footer>

</body>
</html>
